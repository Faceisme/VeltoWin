<Window x:Class="Velto.UI.TrailOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        IsHitTestVisible="False"
        Focusable="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        Visibility="Hidden">
    <Canvas x:Name="HostCanvas">
        <Polyline x:Name="TrailLine"
                  Stroke="#00B8B8"
                  StrokeThickness="4"
                  StrokeLineJoin="Round"
                  StrokeStartLineCap="Round"
                  StrokeEndLineCap="Round"/>
        <Ellipse x:Name="HeadDotOuter"
                 Width="14" Height="14"
                 Fill="White"/>
        <Ellipse x:Name="HeadDotInner"
                 Width="10" Height="10"
                 Fill="#00B8B8"/>
    </Canvas>
</Window>
